import React from 'react';
import {Text, View} from 'react-native';

const PrivacyScreen = () => {
    return (
      <View style={{flex: 1, padding: 10, alignItems: 'center', justifyContent: 'center', backgroundColor: '#000000'}}>
        <Text style={{color:'#fff',fontSize: 20 ,textAlign: 'center'}}> Privacy pdf will be retrieved from website </Text>
      </View>
    );
  };

export default PrivacyScreen;